window.addEventListener('load', (event) => {
  if (window.screen.width <= 500) {
    location.assign('/');
  }
});

window.addEventListener('load', (event) => {
  if (window.screen.width <= 500) {
    location.assign('/');
  }
});
